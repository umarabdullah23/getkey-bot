# getkey-bot

Deploy-only mirror of the GameLoop #get-key auto-grant bot (discord.js).
Secrets are env vars only: `DISCORD_BOT_TOKEN`, `GRANT_SECRET`, `BACKLOG_LIMIT=0`.
Start: `node bot.js`. See the main private repo for full docs.
